This directory is reserved for deployment templates copied into release
packages. Service and Caddy configurations are rendered by
Install-BankingMcp.ps1 because they contain installation-specific paths and
the custom domain.
